machine:
  node:
    version: 7.7.2

dependencies:
  pre:
    - sudo pip install awscli
    - sudo npm install -g webpack

database:
  override:
    - echo 'Skipping database'
test:
  override:
    - echo 'Skipping testing'

deployment:
  production:
    branch: dev
    commands:
      - >
        ./deploy.sh
        
