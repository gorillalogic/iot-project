machine:
  node:
    version: 7.7.2

dependencies:
  override:
    - npm install 
    - sudo npm install webpack -g
  pre:
    - sudo pip install awscli

database:
  override:
    - echo 'Skipping database'
test:
  override:
    - echo 'Skipping testing'

deployment:
  production:
    branch: dev
    commands:
      - >
        ./deploy.sh
        
